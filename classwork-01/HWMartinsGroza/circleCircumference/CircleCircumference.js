function calculateCircleCircumference(circleRadius) {
    return 2 * Math.PI * circleRadius;
}

const radius = 5;
const circleCircumference = calculateCircleCircumference(radius);
console.log(`Circle Circumference is ${circleCircumference}`);