function calculateReactanglePerimeter(lenght, width) {
    return lenght * width;
}

const lenght = 10;
const width = 15;
const perimeter = calculateReactanglePerimeter(lenght, width);
console.log(`Rectangle Perimeter is ${perimeter}`);